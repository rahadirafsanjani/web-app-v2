<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this posts from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, class: "label" %>
    <%= form.text_field :title, class: "control input" %>
  </div>

  <div class="field">
    <%= form.label :post_body, class: "label" %>
    <%= form.text_field :post_body, class: "control textarea" %>
  </div>

  <div class="field">
    <%= form.label :source, class: "label" %>
    <%= form.text_field :source, class: "control input" %>
  </div>

  <div>
    <%= form.submit class: ""%>
  </div>
<% end %>